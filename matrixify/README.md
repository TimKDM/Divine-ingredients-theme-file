# Matrixify import files

Generated by mapping Alisha's "By Type" + "By Function" spreadsheets against
a live Matrixify product export from the store.

## Files

| File | What it does |
|---|---|
| `products-update.csv` | **36** existing products. `Command: MERGE` + `Tags Command: MERGE`. Adds Function_* / Type_* / Category_* tags. Existing tags preserved. |
| `products-new.csv` | **8** new tea products created in `draft` mode with blank Body HTML / SKU / Price. Tags + smart collection memberships are pre-filled from the spreadsheets. |
| `collections.csv` | **17** smart collections (5 By Function, 7 By Type incl. Boba Boba, 5 category-level). |
| `menus.csv` | Main menu: Shop by Function submenu, Shop by Type submenu (Hemp/Oolong hidden), Powders / Spices / Founders' top-level. |
| `products.csv` | Original full import file from the very first generation. Kept for reference only. |

## Upload order

1. **Backup**: Matrixify → Export → "All products + all collections + all menus" → XLSX → keep it.
2. **`collections.csv`**: Matrixify → Import → upload → **dry run** → verify → real run.
3. **`products-update.csv`**: Import → upload → dry run → real run. Updates 36 existing products' tags only.
4. **`products-new.csv`**: Import → upload → dry run → real run. Creates 8 new draft products.
5. **`menus.csv`**: Import → upload → dry run → real run. Rebuilds the Main menu.

## The 8 new draft products

Created with `Status: draft` and `Published: FALSE` — they won't appear on the storefront until Alisha fills in body copy, prices, SKUs, and images and toggles them active.

| Product | Type | Function | Suggested follow-up |
|---|---|---|---|
| Pumpkin Chai Black Tea | Black Tea | — | Body, price, image, SKU |
| Tulsi Chai | Chai | — | Body, price, image, SKU |
| Herbal Chai | Chai | — | Body, price, image, SKU + ingredient confirmation (flagged in spreadsheet) |
| Kashmiri Green Chai | Chai | — | Body, price, image, SKU |
| Sweetened Matcha Green Tea | Matcha | — | Body, price, image, SKU + ingredient confirmation (flagged in spreadsheet) |
| Ube Matcha Tea | Matcha | — | Body, price, image, SKU |
| Unflavoured Black Iced Tea | Iced Tea | — | Body, price, image, SKU |
| Crimsonberry Tea | Iced Tea | — | Body, price, image, SKU |

## Skipped on purpose

- **"Japnese Hojicha Green Tea"** — typo in source spreadsheet. The store already has the product under the correct spelling ("Japanese Hojicha"), and that existing product is included in `products-update.csv` with the correct tags. No new product created.

## Name variant resolved

- The spreadsheet's **"Organic Chamomile Care Herbal Tea"** maps to the existing **`chamomile-care-herbal-tea`** product in the store (manual override). Function_Wind down + Function_Gut Reset tags applied to that existing handle.

## Tag taxonomy

| Tag prefix | Drives smart collection |
|---|---|
| `Function_Immunity`, `Function_Wind down`, `Function_Gut Reset`, `Function_Focus and Energy`, `Function_Detox` | The 5 By-Function collections |
| `Type_Green Tea`, `Type_Black Tea`, `Type_Herbal Tea`, `Type_Chai`, `Type_Matcha`, `Type_Iced Tea`, `Type_Hemp Tea`, `Type_Oolong Tea`, `Type_Boba Boba` | The By-Type collections |
| `Category_Tea`, `Category_Powder`, `Category_Spices_Botanicals` | Top-level category collections |
| `Organic` | All Organic |
| `Founders_Favourite` | Founders' Favourites |

## Notes

- **Boba Boba** is set up as its own type/collection per client direction. No products tagged yet; tag any future Boba product with `Type_Boba Boba` and it auto-populates.
- **Hemp Tea and Oolong Tea** collections are kept with "(planned for removal in future cleanup)" body note. Omitted from the menu but still URL-accessible.
- **Tag policy**: `Tags Command: MERGE` everywhere. Merchant-added tags (e.g. `bundle`, `spring-launch`) are preserved.
- **Products not in the spreadsheets** (~110 powders, spices, botanicals): untouched. Their existing tags and collection memberships stay exactly as they are.
